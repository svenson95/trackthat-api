spring.application.name=trackthat-backend
server.address=0.0.0.0
server.port=8080
spring.data.mongodb.uri=${MONGODB_URI}
debug=true

google.client-id=${GOOGLE_CLIENT_ID}
jwt.secret=${JWT_SECRET}

# for dev
# spring.profiles.active=local
